{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <div class="content">
        <div class="form-container">
            <h1>{{ title }}</h1>
            {{ form_start(form) }}
            {{ form_end(form) }}
        </div>    
    </div>
{% endblock %}
